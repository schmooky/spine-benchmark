{
  "name": "@spine-benchmark/calibration-primitives",
  "version": "0.1.0",
  "description": "Procedurally-generated single-axis Spine skeletons for cost calibration: a pure-fill primitive (coverage/overdraw dial, ~no constraints) and a pure-compute primitive (physics/IK/deform dial, ~no pixels). Lets the fit recover the rendering vs computational coefficients cleanly.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && tsc -p tsconfig.build.json",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "private": true
}
