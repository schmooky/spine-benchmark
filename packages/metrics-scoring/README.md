# @spine-benchmark/metrics-scoring

Score formulas and impact helpers for Spine metrics.

## Exports

- score calculators (`calculateMeshScore`, `calculateClippingScore`, `calculateBlendModeScore`, `calculateBoneScore`, `calculateConstraintScore`, `calculateOverallScore`)
- score/impact UI helpers (`getScoreColor`, `getScoreRating`, `getScoreInterpretation`, `getImpactFromCost`, `getImpactBadgeClass`)
- impact aggregators (`worstRenderingImpact`, `worstComputationalImpact`)
